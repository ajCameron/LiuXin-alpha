# SPDX-FileCopyrightText: 2021 - 2023 Mewbot Developers <mewbot@quicksilver.london>
#
# SPDX-License-Identifier: BSD-2-Clause

name: Auto-Review
run-name: "Automated code review for \"${{ github.head_ref }}\""

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: review-${{ github.head_ref }}
  cancel-in-progress: true

env:
  # Which version of the reports Sonar receives.
  DEFAULT_OS: ubuntu
  DEFAULT_PYTHON: "3.12"

jobs:
  test:
    name: ${{ matrix.name }} Python code (ubuntu/py${{ env.DEFAULT_PYTHON }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        name: ["lint", "test"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python ${{ env.DEFAULT_PYTHON }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.DEFAULT_PYTHON }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            requirements-dev.txt

      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run lint smoke
        if: matrix.name == 'lint'
        shell: bash
        run: python -m compileall -q src tests scripts

      - name: Run focused tests
        if: matrix.name == 'test'
        env:
          PYTHONPATH: src
        shell: bash
        run: |
          pytest -q \
            tests/metadata/containers \
            tests/metadata/api \
            tests/databases/caches/test_cache_plugin_contract.py

  tests-passed:
    name: Tests Passed
    runs-on: ubuntu-latest

    needs:
      - test

    steps:
      - name: Mark all test runs as successful
        run: /bin/true
