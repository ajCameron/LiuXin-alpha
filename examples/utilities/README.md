# Utility examples

`comments_to_html_example.py` converts plain-text comments into the minimal
HTML representation used by the library layer.

```bash
python examples/utilities/comments_to_html_example.py \
  --text $'First paragraph\n\nSecond paragraph'
```
