# Metadata examples

These examples make network requests unless only `--help` is requested:

- `metadata_identify_example.py` runs the enabled identify pipeline.
- `google_books_plugin_example.py` queries Google Books directly.
- `openlibrary_plugin_example.py` fetches OpenLibrary cover metadata and bytes.

Use their timeout and result-limit options when incorporating them into a
diagnostic workflow.
