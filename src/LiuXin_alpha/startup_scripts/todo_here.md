To do

1) Check that LiuXin_data exists
2) Load the data sources available and check that they exist
3) check that covers exists in LiuXin_data
4) check a random selection of cover which are believed to be local (to see if they are)
5) check that the claimed data sources exist
6) check folder stores and update their status
7) fire up the GUI (once it actually exists)
8) check that the database naming scheme is being followed. Issue a warning if it isn't.
9) Check that the language table is properly loaded